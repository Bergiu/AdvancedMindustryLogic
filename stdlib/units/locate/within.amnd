# Checks if the unit in @unit is in the range of the coordinates.
#
# Output:
#   within::is_within: True when @unit is within the range of the coordinates

function within(x, y, range) {
    ucontrol within x y range within::is_within 0
}
